import { ArchiveBadgePipe } from './archive-badge.pipe';

describe('ArchiveBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new ArchiveBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
